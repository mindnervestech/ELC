import ReactPixel from 'react-facebook-pixel';

export const initializeF = () => {
   // ReactPixel.init('3053029494769114');
}

export const pageViewF = () => {
   // ReactPixel.pageView(); 
}

export const trackF = (name, value) => {
   // ReactPixel.track(name, value);
}